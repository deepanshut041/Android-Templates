<template format="4"
        revision="1"
        name="MVVM Basic Fragment"
        description="Creates a new MVVM classes - Fragment, ViewModel, Dagger Module, Status.">

    <category value="Fragment"/>

    <parameter id="className"
        name="Functionality Name"
        type="string"
        constraints="class|unique|nonempty"
        default="Main"
        help="The name of the functionality that requires MVVM views"/>

    <parameter id="newPackage"
        name="Package Name"
        type="string"
        constraints="nonempty"
        default="main"
        help="The name of package in which view to be placed"/>

    <parameter
        id="layoutName"
        name="Layout Name"
        type="string"
        constraints="layout|unique|nonemptnonemptyy"
        default="fragment_main"
        help="The name of the layout to create for the activity" />

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>